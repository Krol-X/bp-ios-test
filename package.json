{
  "name": "bp-ios-test",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "prettier --check .",
    "format": "prettier --write src",
    "format:watch": "onchange \"src/**/*.js\" \"src/**/*.ts\" \"src/**/*.json\" -- prettier --write {{changed}}"
  },
  "devDependencies": {
    "onchange": "^7.1.0",
    "prettier": "^3.2.5",
    "sass": "^1.72.0",
    "sass-mediascreen": "^1.0.4",
    "vite": "^5.2.0"
  }
}
